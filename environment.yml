name: sonify
channels:
  - conda-forge
  - defaults
dependencies:
  - ffmpeg
  - obspy
  - pip
  - setuptools<58.4.0  # https://github.com/pypa/setuptools/issues/2497
  - tqdm
  - pip:
    - --editable .
