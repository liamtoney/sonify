name: sonify
channels:
  - conda-forge
  - defaults
dependencies:
  - ffmpeg
  - obspy
  - pip
# Lines above should be IDENTICAL to environment.yml
  - black
  - imagemagick
  - ipython
  - isort
  - pytest-cov
  - pytest-mpl
  - pip:
    - --requirement doc/requirements.txt
    - --editable .
